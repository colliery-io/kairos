---
id: m4-cli-command-tree-over-kairos
level: task
title: "M4: CLI - command tree over kairos-client"
short_code: "KAIROS-T-0037"
created_at: 2026-07-10T22:02:30.981283+00:00
updated_at: 2026-07-14T23:00:22.531317+00:00
parent: KAIROS-I-0004
blocked_by: [KAIROS-T-0036]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: KAIROS-I-0004
---

# M4: CLI - command tree over kairos-client

## Parent Initiative

[[KAIROS-I-0004]]

## Objective

The full `kairos` command tree per A-0015 over kairos-client: nouns mirroring the API.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [x] Subcommands: `orgs`, `boards` (list/show incl. items-by-column), `strategies|initiatives|tasks|documents|adrs` (list/get/create/edit/transition/delete with confirm), `search` (the S-0005 body via flags or --query-json), `teams`, `streams`, `members`, `admin tenants` (deployment-admin ops)
- [x] Human-readable table output by default (aurora-dark-agnostic terminal styling optional), `--json` everywhere; 409 conflicts render current-version guidance; 422 transition errors list allowed targets
- [x] Every subcommand has help text; `kairos --help` tree is coherent
- [x] Integration test: golden path against compose (login via pre-seeded token → create initiative → create task → transition → search → delete) asserting exit codes and output shapes

## Implementation Notes

References A-0015, S-0005. Thin veneer over kairos-client — no business logic in the CLI.

## Verification Gate (KAIROS-A-0012)

`cargo fmt --check` + `cargo clippy --workspace --all-targets -- -D warnings` clean · `angreal test unit` + `angreal test integration` green · every acceptance criterion demonstrated with command + recorded output in Status Updates · new behavior ships with new tests.

## Status Updates

- 2026-07-10: Created at I-0004 decompose (todo).
- 2026-07-14: Active. Read A-0015 (command tree contract), S-0005 (API surface), kairos-client (client.rs ~70 methods, typed Error with Conflict.current / InvalidTransition.allowed_targets / Forbidden.capability), T-0036's fresh CLI code + live-test recipe. Plan: kairos-cli gains src/table.rs (tiny aligned-column helper, no TUI dep), src/context.rs (Common {--url,--tenant,--json} flattened into every leaf + client builder over CachedTokenProvider), src/commands/{entities,boards,orgs,search,teams,streams,members,admin}.rs. Entity families (strategies/initiatives/tasks/adrs = list/get/create/edit/transition/delete; documents = same minus transition, no such endpoint) generated by one macro over shared ListArgs/GetArgs/EditArgs/TransitionArgs/DeleteArgs + per-family create args; edit fetches current entity, bases the PATCH on its version unless --version overrides (the stale-409 test lever); delete requires --confirm client-side. boards list/show (show = /items grouped by column). orgs show = whoami's organization (gap: no /api/orgs endpoint in S-0005 — recorded, derived from whoami). search: --query/--type/--board/--column/--team/--task-type/--metadata k=v/--after/--before/--is-bucket/--include-deleted/--sort/--order/--limit/--offset + traverse --from/--from-id/--relationships/--direction/--depth + --query-json escape hatch (mutually exclusive with the flags). teams/streams with nested members/teams subcommands; members list/add/set-role/remove (LAST_ADMIN surfaced with guidance); admin tenants list/create/delete --confirm (sends ?confirm=true; deployment-admin 403 hint keyed off details.required=deployment_admin). error.rs upgraded: 409 renders server-current version guidance, 422 INVALID_TRANSITION lists allowed targets name+id, 403 names details.required_capability. Verification: unit tests (parse tree, table, edit request build, search flag-to-body build, error renderings) + tests/cli_tree_live.rs golden path on scratch DB kairos_cli_t0037_test (headless device login, boards list/show, initiative create, task create, invalid transition asserting allowed-targets, valid transition, search, stale-edit 409, edit, delete --confirm, members list), plus a manual live transcript with kairos-server on 127.0.0.1:18094 / scratch DB kairos_t0037_live incl. admin tenants with KAIROS_DEPLOYMENT_ADMINS.
- 2026-07-14: SHIPPED (crates/kairos-cli only; kairos-client untouched). New: src/table.rs (aligned-column renderer, no TUI dep), src/context.rs (Common --url/--tenant/--json flattened into every leaf; client builder over CachedTokenProvider), src/commands/{entities,boards,orgs,search,teams,streams,members,admin}.rs; main.rs wires the tree. Entity families via one macro (`entity_family_cli!`) over shared List/Get/Edit/Transition/Delete args + per-family create args and an EntityView rendering trait over the 5 DTOs; documents omit transition (no S-0005 endpoint). Edit = fetch current → PATCH on its version unless --version; deletes (entities, teams, streams, members remove, admin tenants delete) all gated on --confirm client-side; admin tenants delete then sends ?confirm=true. error.rs upgraded: 409 renders "server-current version is N (title …)" + re-base guidance; 422 INVALID_TRANSITION renders "Allowed target columns: - Name (id)" (empty → explicit none allowed); 403 names details.required_capability, or the KAIROS_DEPLOYMENT_ADMINS hint when details.required=deployment_admin, else the org-admin membership hint; 422 LAST_ADMIN gets keep-one-admin guidance. Gotcha for future macro authors: clap derive cannot evaluate `#[doc = concat!(…)]` (tokens reach the derive unexpanded, help silently falls back to the wrapped struct's doc) — use `#[command(about = concat!(…))]`. Cargo.toml: workspace-only additions (utoipa needed because ListEnvelope<T: ToSchema> leaks the bound into generic helpers). kairos-client gaps recorded: (1) no /api/orgs endpoint in S-0005 — `kairos orgs show` derives from /api/whoami; (2) none otherwise — every noun mapped 1:1 onto existing client methods.
- 2026-07-14: TESTS. Unit now 22 (T-0036's 9 + smoke + new: table rendering x2, command-tree parse, edit-request building incl. --version override + nothing-to-edit, delete --confirm guard, search flag→S-0005 body build x3 incl. metadata k=v / traverse validation / --query-json conflict+malformed, error renderings x5). New integration tests/cli_tree_live.rs (scratch DB kairos_cli_t0037_test, tenant cli_t37, production router in-process on an ephemeral port against live Dex, headless device-flow login, real binary via CARGO_BIN_EXE): boards list (table has initiatives/strategy/platform-delivery) → boards show (== Backlog/== Todo sections; created task appears under Backlog) → initiatives create (human "Created initiative …", code parsed) → tasks create --json (DTO: bug, Backlog column, version 1) → tasks transition invalid Backlog→Active (exit 1, "invalid transition (422)", "Allowed target columns:", "Todo (<uuid>)") → valid Backlog→Todo (exit 0, exact rendering) → search --query/--type (finds task, filters initiative), --json (both groups typed), --query-json → edit --json (version 2) → stale edit --version 1 (exit 1, "conflict (409", "server-current version is 2", --version guidance) → re-based edit (version 3) → delete without --confirm (exit 1) → delete --confirm (exit 0, "Deleted <code>") → get after delete (exit 1) → members list (alice admin) → orgs show → teams list. `cargo test -p kairos-cli`: 22 unit + cli_live (T-0036, still green) + cli_tree_live, all passing (~13s); scratch DBs dropped in-test.
- 2026-07-14: LIVE TRANSCRIPTS (scratch DB kairos_t0037_live; target/debug/kairos-server serve on 127.0.0.1:18094, OIDC_ISSUER_URL=http://localhost:5558/dex, OIDC_AUDIENCE=kairos-cli, KAIROS_BASE_DOMAIN=kairos.test, KAIROS_DEPLOYMENT_ADMINS=alice's Dex sub CiQwOGE4…bG9jYWw; headless device login user_code=SNGF-RDWB → exit 0). (1) `admin tenants list` → "(none)" then `create --slug t0037` → "Provisioned tenant t0037 (schema org_t0037) / boards created: strategy, initiatives, adrs / templates copied: 6 … / initial admin: alice@kairos.test (…)" → list shows SLUG/NAME/SCHEMA=ok; delete without --confirm → exit 1. (2) whoami/orgs show(+--json) → t0037/admin. (3) teams create (delivery board created) / teams list table / streams create / streams teams add+list / teams members add+list / members list (alice admin). (4) members set-role demote → exit 1 "422 LAST_ADMIN … An organization must keep at least one admin"; members remove w/o --confirm → exit 1. (5) boards list table (4 boards) / boards show renders ==-columns with T0037-T-0001 [bug] under Backlog. (6) initiatives/tasks/strategies/documents(parent=initiative)/adrs create → T0037-{I,T,S,D,A}-0001. (7) invalid transition → exit 1 "Allowed target columns: - Todo (uuid)"; valid → exit 0. (8) search flags + --query-json find the task; traverse query returns "(no matches)" cleanly (no relationships seeded). (9) clean edit → v2; stale --version 1 → exit 1 with server-current v2 + title; get renders detail + content. (10) delete w/o confirm exit 1; --confirm → "Deleted T0037-T-0001"; list "(none)". (11) initiatives delete --confirm; `admin tenants delete t0037 --confirm` → "Dropped tenant t0037".
- 2026-07-14: GATE: `cargo fmt --check` clean · `cargo clippy --workspace --all-targets -- -D warnings` clean · `cargo test -p kairos-cli` all green (22 unit + 2 live integration incl. T-0036's flow). Full `angreal test unit|integration` deliberately deferred (shared-services mode, two concurrent agents; per-task instruction). Cleanup: 18094 server killed, kairos_t0037_live + test scratch DBs dropped; shared kairos DB, Dex (200), and the GUI agent's kairos_t0039_gui untouched.