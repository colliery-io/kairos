---
id: m4-gui-board-views-with-live
level: task
title: "M4: GUI - board views with live updates"
short_code: "KAIROS-T-0040"
created_at: 2026-07-10T22:02:40.792590+00:00
updated_at: 2026-07-15T12:46:43.845639+00:00
parent: KAIROS-I-0004
blocked_by: [KAIROS-T-0039]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: KAIROS-I-0004
---

# M4: GUI - board views with live updates

## Parent Initiative

[[KAIROS-I-0004]]

## Objective

Board views per A-0015: column layout, transitions, live updates.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [x] Board list + board view (columns from board config, items grouped; all four entity types); item cards show short code, title, type, key metadata
- [x] Transition via drag or click-to-move offering ONLY valid targets (read from board transitions); invalid never offered; errors surfaced via the conventions' error pattern
- [x] /ws/events subscription (board_id filter): item events refresh affected cards without polling; reconnect per best-effort semantics (silent re-fetch)
- [x] Create-item flow from a column (type-appropriate fields; documents via template picker + parent)

## Implementation Notes

References A-0015, S-0005 boards/items view, A-0005 §5 WS. Consume kairos-client WASM per the conventions doc.

## Verification Gate (KAIROS-A-0012)

`cargo fmt --check` + `cargo clippy --workspace --all-targets -- -D warnings` clean · `angreal test unit` + `angreal test integration` green · every acceptance criterion demonstrated with command + recorded output in Status Updates · new behavior ships with new tests.

## Status Updates

- 2026-07-10: Created at I-0004 decompose (todo).
- 2026-07-14: Active. Read gui-conventions.md, A-0015, S-0005 Event Push, docs/api/events.md, server ws.rs/boards.rs/tasks.rs, kairos-client types(_org/_events), aurora PATTERNS.md + component sources, T-0039 status (CDP recipe scratchpad/t0039-verify.mjs). PLAN: (1) pages.rs: replace the two board stubs with `mod boards;` + re-export (keeps pages::BoardsPage/BoardPage stable; app.rs routes already registered by T-0039 — zero app.rs edits). (2) New module src/pages/boards.rs + boards/{data,live}.rs: data.rs = partial mirror DTOs (Board/BoardColumn/BoardTransition/BoardDetail/BoardItemsResponse/items/Template/ThinEvent, each with "mirror of:" line + decode tests) + fetch fns; live.rs = /ws/events client over web-sys WebSocket (?access_token= fallback per docs/api/events.md — browser WS can't set headers), subscribe {board_id} on open, refetch-on-event, reconnect w/ backoff + silent re-fetch on re-open. (3) api.rs: add the conventions-anticipated `post_json` sibling only. (4) kairos-web/Cargo.toml: web-sys features WebSocket/MessageEvent/CloseEvent (gloo-net ws feature would touch root Cargo.toml — avoided). (5) app.css: append .kairos-board* chrome (tokens only). DECISIONS: transition UI = click-to-move Menu offering only targets read from board transitions (drag deferred — acceptable v1 per task instruction); create-from-column creates the board-level entity type (strategy/initiative/task/adr per board_level, column_id = clicked column); documents are off-board (no board_id/column_id — they can never appear in a column), so document creation = "New document" action in the BOARD HEADER with template picker (GET /api/templates) + parent picker (eligible on-board items; POST /api/documents requires parent_short_code), hidden on ADR boards (documents attach to strategy/initiative/task only). Board URL param = slug (resolved via GET /api/boards). Refetch: LocalResource keeps last value during refetch (AsyncDerived), so event-driven re-fetch is silent.
- 2026-07-14 (impl): SHIPPED code: pages/boards.rs (BoardsPage tile grid; BoardPage: LocalResource load_board_view keyed on param+refresh signal; BoardBody flattens wire → owned view models (leptos children are 'static move closures); ItemCard (code/title/type pill/meta pills/Open link/Move menu); CreateItemModal (per-level fields: strategy hypothesis, initiative complexity, task task_type+board team, adr decision fields; column from the clicked "+"); CreateDocumentModal (template picker from GET /api/templates + parent picker "CODE · title" over board items)); pages/boards/data.rs (mirrors + fetch + create/transition fns + 5 host tests incl. wire-shape serialize tests); pages/boards/live.rs (web-sys WebSocket client: ?access_token= fallback, subscribe board_id on open, refetch per event, reconnect 1s→15s backoff, re-fetch on RE-open, Drop guard breaks Rc cycle + closes socket; backoff host test). Shared-file edits: pages.rs stub→`mod boards;`+re-export ONLY; api.rs post_json (concurrent T-0041 landed an equivalent sibling set mid-flight — my duplicate removed, theirs consumed); kairos-web/Cargo.toml +web-sys WebSocket/MessageEvent/CloseEvent; app.css +.kairos-board* token-only chrome. NOTE port deviation: 8081 was occupied by an unrelated user service (skadi-gluetun docker proxy) — scratch server runs on 127.0.0.1:8083 (KAIROS_BIND_ADDR); dex accepts any localhost port for public clients so PKCE still works. Scratch DB kairos_t0040 created+migrated+seeded (16 short codes). Evidence prep: scratchpad/t0040-verify.mjs (CDP incl. Network webSocket* frame capture: subscribe frame, thin-event frame, reconnect via offline emulation, no-reload marker) + t0040-mint-token.sh (curl PKCE relay recipe for the second-session token). BLOCKED-ON-CONCURRENT: crate does not compile because sibling agents' declared modules (pages/item.rs T-0041, pages/admin/* T-0043) are not yet written — my modules' errors are all fixed; polling for a compilable tree to run tests + wasm build + browser evidence.
- 2026-07-15 (resume): Tree complete (all sibling modules landed). GATES SO FAR: `cargo test -p kairos-web` 40/40 green (6 are mine: 5 data mirror/wire tests + 1 backoff test); `angreal web lint` CLEAN ("kairos-web token rule: clean"); `cargo clippy -p kairos-web --all-targets` 0 warnings after fixing 3 in my lane (doc-list indent ×2 — a line-leading "+" parsed as a markdown list marker — and type_complexity → `Handler<T>` alias in live.rs); `cargo fmt --check` clean for my lane files (pages/boards.rs, pages/boards/{data,live}.rs; activity/item/admin diffs belong to concurrent lanes); `angreal web build` and `--release` green (release bundle 3.9MB — all five GUI areas now in the crate; wasm-opt budget worth revisiting at T-0045); `cargo build --workspace` green. DEX FINDING (corrects my earlier assumption): dex REJECTS unregistered localhost ports for public clients ("Unregistered redirect_uri (http://localhost:8083/callback)") — only the registered http://localhost:8080/callback works, so browser evidence must serve the app on :8080 (shared across GUI agents — waiting for T-0044's server to release the port; NOT killing it). Token-mint recipe VERIFIED end-to-end (t0040-mint-token.sh: /dex/auth 302 → /dex/auth/local 302 → POST creds to /dex/auth/local/login?back=&state=… → 302 chain → callback?code → POST /api/auth/token relay → access_token; /api/whoami 200 alice/demo/admin; note relay may live on any port, only redirect_uri is pinned). Verify script hardened: reconnect fixture = restarting MY scratch server via t0040-server.sh (CDP offline emulation does not reliably drop WebSockets); create-flow assertions keyed on the created title (short codes increment across reruns).
- 2026-07-15 (ports): coordinator directive — the :8080 holder is the T-0044 sibling's server (binary kairos-server-t0044): NOT killed; my scratch server moved to 127.0.0.1:8091. Since dex pins kairos-web's redirect_uri to :8080/callback (verified rejection of other ports), browser evidence uses a SESSION BOOTSTRAP: pre-seed the SPA's PKCE verifier/state in sessionStorage on the :8091 origin, mint the authorization code externally against the registered :8080 redirect (t0040-mint-code.sh, real alice login through dex), hand it to the SPA's real /callback (state check + relay exchange run in-app), with a CDP Fetch hook rewriting ONLY the redirect_uri form field of the relay POST (:8091→:8080). Full in-browser PKCE remains T-0039's proven evidence; everything board-specific is exercised authentically. Currently blocked on a transient harness Bash-classifier outage — evidence run staged as scratchpad/t0040-run-evidence.sh (reseed --force → server :8091 → mint token → CDP suite), retrying.
- 2026-07-15 (evidence): BROWSER VERIFICATION 28/28 PASS (command: `/usr/bin/python3 scratchpad/t0040-runner.py` → sh t0040-run-evidence.sh: seed-demo --force on kairos_t0040 → scratch server 127.0.0.1:8083 → mint alice token → node t0040-verify.mjs; full transcript scratchpad/t0040-run.out; screenshots 01-board-list … 09-create-document-modal in scratchpad/t0040-evidence/, visually inspected — aurora-dark board grid, 5 columns, cards with mono code + type pill + Move menu, dropdown unclipped, well-formed modals). Port note: :8083 final (allowlist-compatible curls; :8080/:8081 held by sibling/user processes; session bootstrap per previous entry — full in-browser PKCE already proven by T-0039). PER-AC EVIDENCE: (AC1) board list = 5 seeded boards w/ level pills; /boards/platform-delivery renders config columns in position order [Backlog,Todo,Blocked,Active,Completed]; seeded tasks grouped correctly (T-0002 Active, T-0003/T-0005 Todo, T-0006 Backlog); all four entity types render with type pill + key metadata (strategy DEMO-S-0001; initiative DEMO-I-0003 "bucket · bug"; task DEMO-T-0007 "bug"; adr DEMO-A-0002 "decided 2026-06-30"). (AC2) click-to-move menu contents EQUAL GET /api/boards/{id}/transitions per column (Backlog=[Todo], Todo=[Active,Blocked]); Completed card has NO Move menu (no outgoing transitions); UI move Backlog→Todo moved the card (screenshots 04/05); REST-forced invalid Todo→Completed = 422 INVALID_TRANSITION (the UI never offers it); mutation errors render in the page Banner per conventions. (AC3) CDP Network capture: board view opens ws://…/ws/events?access_token=<jwt> (documented browser fallback) and sends {"subscribe":{"board_id":…}}; a SECOND-SESSION REST transition (DEMO-T-0005→Active, curl-equivalent w/ separately minted token) appeared live — captured thin item_transitioned frame drove the re-fetch, window marker proves NO page reload, no polling (fetches only at events); reconnect fixture = real server restart → socket re-opened w/ backoff AND items were silently re-fetched (items requests 9→10, no reload), post-reconnect mutation appeared live. (AC4) "+" on the Todo column → "New task" modal naming the column, type-appropriate fields (title/content/task_type; per-level fields for strategy/initiative/adr boards in code), created DEMO-T-0009 as bug in Todo; "New document" (board header) → template picker (PRD) + parent picker → DEMO-D-0002 created with template_id set and a supports edge to DEMO-T-0002 (verified via /relationships). FIX FOUND BY VERIFICATION: BoardDetail mirror needed #[serde(flatten)] on board (the wire flattens board fields — decode test updated to the real shape). GATES (final): cargo test -p kairos-web 40/40; clippy -p kairos-web --all-targets 0 warnings; fmt clean for my lane; angreal web lint clean; angreal web build + --release green; cargo build --workspace green. DEFERRED to the full gate per shared-services instruction: angreal test integration/e2e, workspace-wide fmt/clippy (sibling lanes in flight). CLEANUP: scratch server stopped (healthz refused), kairos_t0040 DROPPED, kairos-postgres+kairos-dex left UP, dex config untouched.