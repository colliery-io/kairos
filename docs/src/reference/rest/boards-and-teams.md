<!-- GENERATED by scripts/render-openapi.py from the OpenAPI spec.
     Do not edit: your changes will be overwritten, and CI fails when this
     file disagrees with the spec. To change it, change the
     `#[utoipa::path]` annotation on the handler. -->

# Boards and teams

Board configuration, membership, and the team structures that own boards.

## boards

### `GET /api/boards`

List boards (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_Board`](schemas.md#listenvelope_board) | Page of boards |
| `401` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing/invalid token |

### `POST /api/boards`

Create a board seeded with the system default columns/transitions for
its level (KAIROS-A-0002). Org-admin-only: a new board has no capability
context yet (A-0006 tenant-config fallback).

Request body (required): `application/json`, [`CreateBoardRequest`](schemas.md#createboardrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`BoardDetail`](schemas.md#boarddetail) | Created, with the seeded configuration |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Slug already in use |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad level/team reference |

### `GET /api/boards/{id}`

Board detail: the board plus its columns and transitions (open
tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `include_removed_columns` | path | yes | `boolean` | Also return the columns that have been REMOVED from this board, each carrying `removed_at` (default `false` — a removed column is not part of the board and must not render on one, KAIROS-T-0161).  The one legitimate caller is a reader holding an ARCHIVED item that still points at such a column: "which column was this put away in?" has to stay answerable (KAIROS-A-0020, KAIROS-T-0164). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`BoardDetail`](schemas.md#boarddetail) | The board with columns and transitions |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |

### `DELETE /api/boards/{id}`

Soft-delete a board. Only allowed when NO workflow item references it
(422 `BOARD_NOT_EMPTY` otherwise — the T-0010 empty rule applied at
board scope). Requires `configure_boards` on the board.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Soft-deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | BOARD_NOT_EMPTY |

### `PATCH /api/boards/{id}`

Update board settings (name/slug). Requires `configure_boards` on the
board.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

Request body (required): `application/json`, [`UpdateBoardRequest`](schemas.md#updateboardrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`Board`](schemas.md#board) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Slug already in use |

### `GET /api/boards/{id}/columns`

List a board's columns in position order (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`BoardColumn`](schemas.md#boardcolumn) | Columns in position order |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |

### `POST /api/boards/{id}/columns`

Add a column (T-0010 rules: unique name and position). Requires
`configure_boards` on the board.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

Request body (required): `application/json`, [`CreateColumnRequest`](schemas.md#createcolumnrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`BoardColumn`](schemas.md#boardcolumn) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | DUPLICATE_COLUMN_NAME / DUPLICATE_COLUMN_POSITION / VALIDATION |

### `DELETE /api/boards/{id}/columns/{col_id}`

Remove a column — a soft delete (KAIROS-T-0161). Only allowed when no
LIVE item occupies it; the T-0010 rule surfaces as 422
`COLUMN_NOT_EMPTY`. Archived cards may stay behind and keep reporting
the column they were put away in. Requires `configure_boards`.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `col_id` | path | yes | `string` | Column id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Removed (soft: archived cards keep its name; its transition edges are kept but no longer apply) |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board/column |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | COLUMN_NOT_EMPTY (details.item_count) |

### `PATCH /api/boards/{id}/columns/{col_id}`

Rename and/or move a column (T-0010 rules; moving reorders the board's
columns around the new position). Requires `configure_boards`.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `col_id` | path | yes | `string` | Column id (UUID) |

Request body (required): `application/json`, [`UpdateColumnRequest`](schemas.md#updatecolumnrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`BoardColumn`](schemas.md#boardcolumn) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board/column |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | DUPLICATE_COLUMN_NAME / VALIDATION |

### `GET /api/boards/{id}/items`

All live items on the board, grouped by column (columns in position
order; every entity type — strategies, initiatives, tasks, ADRs). Open
tenant-wide. `?repository=` narrows the tasks (KAIROS-T-0104).

`?include_deleted=true` adds the archived cards back, in the column
each was put away in and marked with `archived_at` (KAIROS-T-0159).
That is an audit view, not a board view — "what was in Done last
quarter?" rather than "what is on the board now?". The children-
progress rollup and the blocks summary are deliberately NOT widened by
it: ADR-20 rule 5 says archived work is not live work, so the counts
keep counting live rows however the listing is asked for.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `repository` | path | yes | `string`, nullable | Narrow the TASKS to those bound to this repository (slug or UUID). Other entity types are unaffected. Unknown repository → 422. |
| `include_deleted` | path | yes | `boolean` | Include archived (put-away) cards, each marked with `archived_at` (KAIROS-A-0020 rule 2). Default false — a board is a live board unless the reader says otherwise (rule 3). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`BoardItemsResponse`](schemas.md#boarditemsresponse) | Items grouped by column |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |

### `GET /api/boards/{id}/transitions`

List a board's allowed transitions (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`BoardTransition`](schemas.md#boardtransition) | Transition edges |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |

### `POST /api/boards/{id}/transitions`

Add a transition edge (T-0010 rules: endpoints on the board, distinct,
not duplicate). Requires `configure_boards`.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

Request body (required): `application/json`, [`CreateTransitionRequest`](schemas.md#createtransitionrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`BoardTransition`](schemas.md#boardtransition) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | DUPLICATE_TRANSITION / VALIDATION |

### `DELETE /api/boards/{id}/transitions/{transition_id}`

Remove a transition edge by id. Requires `configure_boards`.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `transition_id` | path | yes | `string` | Transition id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Removed |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing capability |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board/transition |

## board-members

### `GET /api/boards/{id}/members`

List a board's members and their capability grants (open tenant-wide —
A-0006 auditability).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`BoardMember`](schemas.md#boardmember) | Members with their capabilities |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |

### `POST /api/boards/{id}/members`

Add a member with capabilities (T-0011 grants; duplicate grant → 409).
Requires `manage_members` on the board (or org admin).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |

Request body (required): `application/json`, [`AddBoardMemberRequest`](schemas.md#addboardmemberrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`BoardMember`](schemas.md#boardmember) | Granted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing manage_members |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | A capability was already granted |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown user or capability outside the A-0006 vocabulary |

### `DELETE /api/boards/{id}/members/{user_id}`

Remove a member: revoke ALL their capabilities on the board (T-0011
revokes, one activity row each). Requires `manage_members` (or org
admin).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `user_id` | path | yes | `string` | User id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`RemoveBoardMemberResponse`](schemas.md#removeboardmemberresponse) | All grants revoked |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing manage_members |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board, or user is not a member |

### `PATCH /api/boards/{id}/members/{user_id}`

Replace a member's capability set (revokes what is absent, grants what
is new — each change is a T-0011 grant/revoke with its activity row).
Requires `manage_members` on the board (or org admin).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Board id (UUID) |
| `user_id` | path | yes | `string` | User id (UUID) |

Request body (required): `application/json`, [`ReplaceCapabilitiesRequest`](schemas.md#replacecapabilitiesrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`BoardMember`](schemas.md#boardmember) | The member's new capability set |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing manage_members |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown board, or user is not a member |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Capability outside the A-0006 vocabulary |

## teams

### `GET /api/teams`

List teams (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_Team`](schemas.md#listenvelope_team) | Page of teams |
| `401` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing/invalid token |

### `POST /api/teams`

Create a team AND its delivery board (seeded from the system delivery
defaults, slug `{slug}-delivery`) in one transaction. Org-admin-only.

Request body (required): `application/json`, [`CreateTeamRequest`](schemas.md#createteamrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`Team`](schemas.md#team) | Created; delivery_board_id names the team's new board |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Team or board slug already in use |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad team_type |

### `GET /api/teams/by-slug/{slug}`

One live team by SLUG (open tenant-wide; KAIROS-T-0083 — the web
client resolves `/teams/:slug` here instead of scanning the list).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `slug` | path | yes | `string` | Team slug |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`Team`](schemas.md#team) | The team |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown slug |

### `GET /api/teams/{id}`

Get one team (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`Team`](schemas.md#team) | The team |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `DELETE /api/teams/{id}`

Soft-delete a team and its delivery board together. The board must be
empty (422 `BOARD_NOT_EMPTY` — the T-0010 empty rule). Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Team and delivery board soft-deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | BOARD_NOT_EMPTY |

### `PATCH /api/teams/{id}`

Update a team (name/slug/team_type). Org-admin-only. The delivery
board's name/slug are independent and unchanged.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

Request body (required): `application/json`, [`UpdateTeamRequest`](schemas.md#updateteamrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`Team`](schemas.md#team) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Slug already in use |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad team_type or empty body |

### `GET /api/teams/{id}/links`

The team's in-flight forge links (KAIROS-T-0101): links whose item is
a task of this team, or an item on the team's delivery board, or whose
repository is attributed to the team. Open tenant-wide.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `state` | path | yes | `string`, nullable | Comma-separated states; defaults to `open,draft` — the panel's question is "what is in flight", not merged history. |
| `limit` | path | yes | `integer`, nullable | Result cap (default 100). |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`TeamLink`](schemas.md#teamlink) | In-flight links, newest first |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `GET /api/teams/{id}/members`

List a team's members (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`TeamMember`](schemas.md#teammember) | Members |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `POST /api/teams/{id}/members`

Add a member to a team. Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

Request body (required): `application/json`, [`AddTeamMemberRequest`](schemas.md#addteammemberrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`TeamMember`](schemas.md#teammember) | Added |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Already a member |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown user |

### `DELETE /api/teams/{id}/members/{user_id}`

Remove a member from a team. Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `user_id` | path | yes | `string` | User id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Removed |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team or not a member |

### `GET /api/teams/{id}/work-documents`

The documents attached to the team's WORK (KAIROS-T-0084): live
documents whose supports-parent is a task of the team or an item on
the team's delivery board. Open tenant-wide. Docs under org-level
items deliberately absent — team attribution follows the parent item.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`TeamWorkDocument`](schemas.md#teamworkdocument) | Derived work documents, by short code |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

## team-pages

### `GET /api/teams/{id}/announcements`

The team's announcements, pinned first then newest (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`TeamAnnouncement`](schemas.md#teamannouncement) | Announcements |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `POST /api/teams/{id}/announcements`

Post an announcement (team member or org admin; append-only — there
is no edit route).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

Request body (required): `application/json`, [`CreateTeamAnnouncementRequest`](schemas.md#createteamannouncementrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`TeamAnnouncement`](schemas.md#teamannouncement) | Posted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a team member |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Body too large |

### `DELETE /api/teams/{id}/announcements/{announcement_id}`

Delete an announcement (its author or an org admin).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `announcement_id` | path | yes | `string` | Announcement id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not the author (and not org admin) |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team or announcement |

### `GET /api/teams/{id}/pages`

The team's live page tree as a flat list (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`TeamPage`](schemas.md#teampage) | The page tree, flat |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `POST /api/teams/{id}/pages`

Create a page or folder (team member or org admin).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |

Request body (required): `application/json`, [`CreateTeamPageRequest`](schemas.md#createteampagerequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`TeamPage`](schemas.md#teampage) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a team member |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad kind/parent, sibling slug conflict, or content too large |

### `GET /api/teams/{id}/pages/{page_id}`

One live page (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `page_id` | path | yes | `string` | Page id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`TeamPage`](schemas.md#teampage) | The page |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team or page |

### `DELETE /api/teams/{id}/pages/{page_id}`

Soft-delete a page (folders must be empty; the Charter never).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `page_id` | path | yes | `string` | Page id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a team member |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team or page |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Protected page or non-empty folder |

### `PATCH /api/teams/{id}/pages/{page_id}`

Edit a page: EITHER a version-checked content save OR a rename/move —
not both in one call.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Team id (UUID) |
| `page_id` | path | yes | `string` | Page id (UUID) |

Request body (required): `application/json`, [`UpdateTeamPageRequest`](schemas.md#updateteampagerequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`TeamPage`](schemas.md#teampage) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a team member |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team or page |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Stale version; details.current carries the current page |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Mixed edit kinds, protected page, slug conflict, or bad parent |

## delivery-streams

### `GET /api/delivery-streams`

List delivery streams (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_DeliveryStream`](schemas.md#listenvelope_deliverystream) | Page of streams |
| `401` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing/invalid token |

### `POST /api/delivery-streams`

Create a delivery stream. Org-admin-only.

Request body (required): `application/json`, [`CreateStreamRequest`](schemas.md#createstreamrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`DeliveryStream`](schemas.md#deliverystream) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Slug already in use |

### `GET /api/delivery-streams/{id}`

Get one delivery stream (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`DeliveryStream`](schemas.md#deliverystream) | The stream |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream |

### `DELETE /api/delivery-streams/{id}`

Soft-delete a delivery stream (team links are left in place — the
stream is recoverable). Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Soft-deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream |

### `PATCH /api/delivery-streams/{id}`

Update a delivery stream. Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |

Request body (required): `application/json`, [`UpdateStreamRequest`](schemas.md#updatestreamrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`DeliveryStream`](schemas.md#deliverystream) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Slug already in use |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Empty body |

### `GET /api/delivery-streams/{id}/teams`

Teams in this stream (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | array of [`Team`](schemas.md#team) | Teams in the stream |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream |

### `POST /api/delivery-streams/{id}/teams`

Add a team to a stream. Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |

Request body (required): `application/json`, [`AddStreamTeamRequest`](schemas.md#addstreamteamrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Added |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Team already in the stream |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown team |

### `DELETE /api/delivery-streams/{id}/teams/{team_id}`

Remove a team from a stream. Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Stream id (UUID) |
| `team_id` | path | yes | `string` | Team id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgDeleteResponse`](schemas.md#orgdeleteresponse) | Removed |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown stream or team not in stream |

## members

### `GET /api/members`

List the organization's members (open tenant-wide, paginated, ordered
by email).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_OrgMember`](schemas.md#listenvelope_orgmember) | Page of members |
| `401` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Missing/invalid token |

### `POST /api/members`

Add a member by email. The user must have logged in once already (JIT
provisioning creates `public.users` rows at first login) — unknown
email is 404 with that guidance. Org-admin-only.

Request body (required): `application/json`, [`AddOrgMemberRequest`](schemas.md#addorgmemberrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`OrgMember`](schemas.md#orgmember) | Added |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | No user with this email has logged in yet |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Already a member |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad role |

### `DELETE /api/members/{user_id}`

Remove a member. Removing the LAST admin is rejected (422 `LAST_ADMIN`).
Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `user_id` | path | yes | `string` | User id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`RemoveOrgMemberResponse`](schemas.md#removeorgmemberresponse) | Removed |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a member |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | LAST_ADMIN |

### `PATCH /api/members/{user_id}`

Change a member's role. Demoting the LAST admin is rejected (422
`LAST_ADMIN`). Org-admin-only.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `user_id` | path | yes | `string` | User id (UUID) |

Request body (required): `application/json`, [`UpdateOrgMemberRequest`](schemas.md#updateorgmemberrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`OrgMember`](schemas.md#orgmember) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Not a member |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | LAST_ADMIN or bad role |

