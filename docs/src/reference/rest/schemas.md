<!-- GENERATED by scripts/render-openapi.py from the OpenAPI spec.
     Do not edit: your changes will be overwritten, and CI fails when this
     file disagrees with the spec. To change it, change the
     `#[utoipa::path]` annotation on the handler. -->

# Schemas

The wire shapes the operations reference, alphabetically. A field is optional unless marked required.

## ActivityEntry

One `activity_log` row, as returned by `GET /api/activity`.

| Field | Type | Required | Description |
|---|---|---|---|
| `action` | `string` | yes | `transition|create|delete|relationship_add|relationship_remove|capability_grant|capability_revoke|board_config`. |
| `actor_id` | `string` | yes | Who did it (user UUID). |
| `details` | `string` | yes | Structured context, e.g. `"column:Draft->Active"`. |
| `entity_id` | `string`, nullable | no | The item acted on (UUID; null for relationship actions). |
| `entity_type` | `string`, nullable | no | `strategy|initiative|task|document|adr` (null for relationship actions). |
| `id` | `string` | yes | Row id (UUID). |
| `occurred_at` | `string` | yes | RFC 3339. |

## AddBoardMemberRequest

Body of `POST /api/boards/{id}/members`.

| Field | Type | Required | Description |
|---|---|---|---|
| `capabilities` | array of `string` | yes | Capabilities to grant (KAIROS-A-0006 vocabulary or glob; at least one). |
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## AddOrgMemberRequest

Body of `POST /api/members`. The user is resolved by email from `public.users` — users are JIT-provisioned at first login, so an unknown email means the person must log in once first (404 with that guidance).

| Field | Type | Required | Description |
|---|---|---|---|
| `email` | `string` | yes |  |
| `role` | `string`, nullable | no | `admin|member`; defaults to `member`. |

## AddStreamTeamRequest

Body of `POST /api/delivery-streams/{id}/teams`.

| Field | Type | Required | Description |
|---|---|---|---|
| `team_id` | `string` | yes | Team id (UUID). |

## AddTeamMemberRequest

Body of `POST /api/teams/{id}/members`.

| Field | Type | Required | Description |
|---|---|---|---|
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## Adr

An Architecture Decision Record, as returned by `/api/adrs`. Board placement is optional (`board_id`/`column_id` both set or both null).

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this work was put away, RFC 3339; absent while it is live. Archiving hides work from default listings and nothing more (KAIROS-A-0020) — anything serving an archived row marks it, so an auditor never mistakes it for live work. |
| `board_id` | `string`, nullable | no | ADR board (UUID), if placed on one. |
| `column_id` | `string`, nullable | no | Current column (UUID), if placed on a board. |
| `content` | `string` | yes | Markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Creator user id (UUID). |
| `decision_date` | `string`, nullable | no | `YYYY-MM-DD`. |
| `decision_maker` | `string`, nullable | no |  |
| `id` | `string` | yes | Entity id (UUID). |
| `short_code` | `string` | yes | Tenant-scoped short code (`{PREFIX}-A-{NNNN}`). |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `updated_by` | `string` | yes | Last editor user id (UUID). |
| `version` | `integer` | yes | Optimistic-concurrency version (KAIROS-A-0004). |

## ApiKeyCreatedResponse

`POST /api/service-accounts/{id}/keys` response — the ONLY place the secret appears.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes |  |
| `expires_at` | `string`, nullable | no |  |
| `id` | `string` | yes |  |
| `key` | `string` | yes | The full API key. Shown once; store it now. |
| `name` | `string` | yes |  |
| `prefix` | `string` | yes |  |

## ApiKeyListResponse

`GET /api/service-accounts/{id}/keys` envelope.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of [`ApiKeyView`](schemas.md#apikeyview) | yes |  |
| `total` | `integer` | yes |  |

## ApiKeyView

One row of `GET /api/service-accounts/{id}/keys` — never a secret or hash.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes |  |
| `expires_at` | `string`, nullable | no |  |
| `id` | `string` | yes |  |
| `last_used_at` | `string`, nullable | no |  |
| `name` | `string` | yes |  |
| `prefix` | `string` | yes |  |
| `revoked_at` | `string`, nullable | no |  |

## BlocksCounts

Dependency counts behind a board card's blocked-by/blocks badges (KAIROS-T-0091).

| Field | Type | Required | Description |
|---|---|---|---|
| `blocked_by` | `integer` | yes | Live incoming `blocks` edges (things blocking this item). |
| `blocks` | `integer` | yes | Live outgoing `blocks` edges (things this item blocks). |

## Board

A board (`/api/boards` list element).

| Field | Type | Required | Description |
|---|---|---|---|
| `board_level` | `string` | yes | `strategy|initiative|delivery|adr`. |
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Board id (UUID). |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `team_id` | `string`, nullable | no | Owning team (UUID); set for delivery boards (KAIROS-A-0002). |
| `updated_at` | `string` | yes | RFC 3339. |

## BoardColumn

A board column.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string` | yes | Owning board (UUID). |
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Column id (UUID). |
| `is_done` | `boolean` | no | Occupants count as completed for children-progress rollups (KAIROS-T-0080). |
| `name` | `string` | yes |  |
| `position` | `integer` | yes | Display ordering, 0-indexed. |
| `removed_at` | `string`, nullable | no | When this column was REMOVED from its board (RFC 3339), absent while it is part of the board (KAIROS-T-0161, KAIROS-T-0164). `GET /api/boards/{id}` omits removed columns entirely unless it is asked for them (`?include_removed_columns=true`), so this is `None` on every default read. It is `Some` only for a caller that wants the column an ARCHIVED card was put away in — the audit fact the soft delete exists to preserve. Named `removed_at`, not `archived_at`: the entity DTOs' `archived_at` is the KAIROS-A-0020 work-item state, and a column is not work. |
| `updated_at` | `string` | yes | RFC 3339. |

## BoardColumnItems

One column's items in the `GET /api/boards/{id}/items` view: every live workflow item of every entity type currently in this column.

| Field | Type | Required | Description |
|---|---|---|---|
| `adrs` | array of [`Adr`](schemas.md#adr) | yes |  |
| `column` | [`BoardColumn`](schemas.md#boardcolumn) | yes |  |
| `initiatives` | array of [`Initiative`](schemas.md#initiative) | yes |  |
| `strategies` | array of [`Strategy`](schemas.md#strategy) | yes |  |
| `tasks` | array of [`Task`](schemas.md#task) | yes |  |

## BoardDetail

Board detail: the board plus its full configuration (`GET /api/boards/{id}`).

## BoardItemsResponse

Response of `GET /api/boards/{id}/items`: all items on the board, grouped by column (columns in position order).

| Field | Type | Required | Description |
|---|---|---|---|
| `blocks_summary` | `object` | no | Blocked-by/blocks counts keyed by short code, for every item on this board with at least one live `blocks` edge (KAIROS-T-0091) — one grouped query; soft-deleted neighbors never count. |
| `board` | [`Board`](schemas.md#board) | yes |  |
| `children_progress` | `object` | no | `(done, total)` direct-children counts keyed by the PARENT item's short code, for every item on this board that has children (KAIROS-T-0080) — computed in one grouped query, never per item. |
| `columns` | array of [`BoardColumnItems`](schemas.md#boardcolumnitems) | yes |  |

## BoardMember

A board member and their capability grants (`GET /api/boards/{id}/members` element).

| Field | Type | Required | Description |
|---|---|---|---|
| `capabilities` | array of `string` | yes | The user's capability grants on this board (sorted). |
| `display_name` | `string` | yes |  |
| `email` | `string` | yes |  |
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## BoardTransition

An allowed column-to-column transition edge.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string` | yes | Owning board (UUID). |
| `from_column_id` | `string` | yes | Source column (UUID). |
| `id` | `string` | yes | Transition id (UUID) — used by `DELETE /api/boards/{id}/transitions/{transition_id}`. |
| `to_column_id` | `string` | yes | Target column (UUID). |

## CascadePreviewResponse

Response of `GET /api/{entity_type}/{short_code}/cascade-preview` (KAIROS-T-0051) — the AUTHORITATIVE KAIROS-A-0001 descendant set a soft-delete of this item WOULD cascade to, computed WITHOUT deleting. The fields mirror [`DeleteResponse`] so a client can render the same warning before the delete as it shows after, and `cascaded_short_codes` is identical to the `DeleteResponse` a subsequent delete would return (barring concurrent edits).

| Field | Type | Required | Description |
|---|---|---|---|
| `cascade_count` | `integer` | yes | How many live descendants a delete would cascade to (root excluded). |
| `cascaded_short_codes` | array of `string` | yes | Short codes of the live descendants a delete would cascade to, sorted. |
| `short_code` | `string` | yes | The item's short code (the cascade root). |

## ChildColumnProgress

One column bucket of a children-progress rollup (KAIROS-T-0080).

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string` | yes | The column's board (children may span boards). |
| `column_id` | `string` | yes | Column id (UUID). |
| `column_name` | `string` | yes |  |
| `count` | `integer` | yes |  |
| `is_done` | `boolean` | yes |  |

## ChildrenProgressResponse

Response of `GET /api/{entity_type}/{short_code}/children-progress` (KAIROS-T-0080): the item's direct `parent`-edge children grouped by their board column. `done` counts children in `is_done` columns; when NO involved column is flagged, clients must show composition only, never a done percentage.

| Field | Type | Required | Description |
|---|---|---|---|
| `by_column` | array of [`ChildColumnProgress`](schemas.md#childcolumnprogress) | yes | Per-column composition, board-then-position order. |
| `done` | `integer` | yes | Children sitting in `is_done` columns. |
| `has_done_columns` | `boolean` | no | False when no board hosting the children has a done-flagged column — show composition only, never a done fraction. |
| `short_code` | `string` | yes | The parent item's short code. |
| `total` | `integer` | yes | Direct live children (soft-deleted excluded; supports/informs material never counts). |

## CreateAdrRequest

Body of `POST /api/adrs`. `board_id`/`column_id` follow the DDL rule: both set (on-board) or both omitted (off-board; creation is then org-admin-only, KAIROS-A-0006 fallback).

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string`, nullable | no | ADR board (UUID); omit for an off-board ADR. |
| `column_id` | `string`, nullable | no | Column (UUID); defaults to the board's first column when `board_id` is set. |
| `content` | `string` | no | Markdown content; defaults to empty. |
| `decision_date` | `string`, nullable | no | `YYYY-MM-DD`. |
| `decision_maker` | `string`, nullable | no |  |
| `title` | `string` | yes |  |

## CreateApiKeyRequest

`POST /api/service-accounts/{id}/keys` body.

| Field | Type | Required | Description |
|---|---|---|---|
| `expires_at` | `string`, nullable | no | Optional RFC 3339 expiry; omitted = never expires. |
| `name` | `string` | yes | Operator label for the key ("gha-main", ...). |

## CreateBoardRequest

Body of `POST /api/boards`: creates a board seeded with the system default columns/transitions for its level (KAIROS-A-0002).

| Field | Type | Required | Description |
|---|---|---|---|
| `board_level` | `string` | yes | `strategy|initiative|delivery|adr`. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `team_id` | `string`, nullable | no | Owning team (UUID) for delivery boards. |

## CreateColumnRequest

Body of `POST /api/boards/{id}/columns`.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | yes |  |
| `position` | `integer` | yes | 0-indexed position; must not collide with an existing column (422 `DUPLICATE_COLUMN_POSITION`). |

## CreateDocumentRequest

Body of `POST /api/documents`. Documents attach to a workflow item at birth: `parent_short_code` is REQUIRED (KAIROS-T-0018 contract) and must name a strategy, initiative, or task; the server creates the `supports` edge and authorizes `manage_documents` against the parent's board.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string`, nullable | no | Markdown content. Omitted + `template_id` set = the template's content is stamped in. |
| `parent_short_code` | `string`, nullable | no | Short code of the workflow item this document supports. Required (422 `VALIDATION` when missing). |
| `template_id` | `string`, nullable | no | Template to stamp content + metadata defaults from (UUID). |
| `title` | `string` | yes |  |

## CreateForgeConnectionRequest

Body of `POST /api/forge-connections`: connect webhooks for a registered repository (slug or UUID). Register the repository first via `POST /api/repositories`; its `forge` must be `github` or `gitlab`.

| Field | Type | Required | Description |
|---|---|---|---|
| `repository` | `string` | yes |  |

## CreateInitiativeRequest

Body of `POST /api/initiatives`.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string` | yes | Board to create the initiative on (UUID). |
| `bucket_type` | `string`, nullable | no | Marks the initiative as a bucket of this kind (`tech_debt|bug|ad_hoc`); `is_bucket` is derived. |
| `column_id` | `string`, nullable | no | Column to place it in (UUID); defaults to the board's first column. |
| `complexity` | `string`, nullable | no | T-shirt sizing (`xs|s|m|l|xl`). |
| `content` | `string` | no | Markdown content; defaults to empty. |
| `title` | `string` | yes |  |

## CreateMetadataDefinitionRequest

Body of `POST /api/metadata-definitions` (org admin).

| Field | Type | Required | Description |
|---|---|---|---|
| `entity_types` | array of `string` | no | Entity types the definition applies to (KAIROS-T-0078); empty = all types. |
| `enum_options` | array of `string` | no | Required non-empty for `enum`; forbidden otherwise. |
| `field_type` | `string` | yes | `string|enum|date`. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |

## CreateRelationshipRequest

Body of `POST /api/relationships` (org admin only, KAIROS-A-0006).

| Field | Type | Required | Description |
|---|---|---|---|
| `relationship` | `string` | yes | `parent|supports|informs|supersedes|blocks`. |
| `source_short_code` | `string` | yes | Short code of the edge's source item (KAIROS-A-0001 orientation). |
| `target_short_code` | `string` | yes | Short code of the edge's target item. |

## CreateRepositoryRequest

Body of `POST /api/repositories`.

| Field | Type | Required | Description |
|---|---|---|---|
| `default_branch` | `string`, nullable | no | Defaults to `main`. |
| `description` | `string`, nullable | no | Short "how to work here" blurb for agents. |
| `forge` | `string` | yes | `github|gitlab|other`. |
| `repo_full_name` | `string` | yes | `owner/repo` — must match what the forge sends in webhook payloads. |
| `repo_url` | `string` | yes | Browser URL of the repository. |
| `slug` | `string`, nullable | no | Tenant-unique slug; defaults to one derived from `repo_full_name` (`acme/payments-api` → `acme-payments-api`). |
| `team` | `string` | yes | The owning team (UUID or slug). Exactly one (KAIROS-A-0019). |

## CreateScimTokenRequest

`POST /api/scim-tokens` body.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | yes | Operator label ("okta-prod", ...). |

## CreateServiceAccountRequest

`POST /api/service-accounts` body.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | yes | Operator label ("ci-deploy", ...). |

## CreateStrategyRequest

Body of `POST /api/strategies`.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string` | yes | Board to create the strategy on (UUID). |
| `column_id` | `string`, nullable | no | Column to place it in (UUID); defaults to the board's first column. |
| `content` | `string` | no | Markdown content; defaults to empty. |
| `hypothesis` | `string`, nullable | no |  |
| `title` | `string` | yes |  |

## CreateStreamRequest

Body of `POST /api/delivery-streams`.

| Field | Type | Required | Description |
|---|---|---|---|
| `description` | `string`, nullable | no |  |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |

## CreateTaskRequest

Body of `POST /api/tasks`.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string`, nullable | no | Board to create the task on (UUID). Optional since KAIROS-T-0104: when `repository` is given the task is ROUTED to the owning team's delivery board (A-0019); when both are given they must agree; neither is a 422. |
| `column_id` | `string`, nullable | no | Column to place it in (UUID); defaults to the board's first column. |
| `content` | `string` | no | Markdown content; defaults to empty. |
| `repository` | `string`, nullable | no | Repository to issue the task against (slug or UUID, KAIROS-T-0104). Routes the task: repo -> owning team -> that team's delivery board. `repository` is THE reference field name on the wire (KAIROS-T-0115); `repository_id` is accepted as an alias for one release. |
| `task_type` | `string`, nullable | no | `task|bug|tech_debt|support`; defaults to `task`. |
| `team_id` | `string`, nullable | no | Owning team (UUID). Defaults to the repository's owning team when `repository` is given; an explicit different team is a 422. |
| `title` | `string` | yes |  |
| `work_class` | `string`, nullable | no | Planned/Support lane (`planned|support`, KAIROS-T-0077). Defaults to `support` when `task_type` is `support`, else `planned`. |

## CreateTeamAnnouncementRequest

Body of `POST /api/teams/{id}/announcements`.

| Field | Type | Required | Description |
|---|---|---|---|
| `body` | `string` | yes | Markdown body. |
| `pinned` | `boolean` | no | Pin it to the top of the feed. |

## CreateTeamPageRequest

Body of `POST /api/teams/{id}/pages`.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | no | Markdown content (pages only); defaults to empty. |
| `kind` | `string` | yes | `folder|page`. |
| `parent_id` | `string`, nullable | no | Parent FOLDER (UUID); omit for a root node. |
| `position` | `integer` | no | Display order among siblings; defaults to 0. |
| `slug` | `string` | yes | URL segment, unique among siblings. |
| `title` | `string` | yes |  |

## CreateTeamRequest

Body of `POST /api/teams`. Creating a team also creates its delivery board (slug `{slug}-delivery`) from the system defaults, in the same transaction.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `team_type` | `string`, nullable | no | `stream_aligned|platform|enabling|complicated_subsystem`; defaults to `stream_aligned`. |

## CreateTemplateRequest

Body of `POST /api/templates` (org admin).

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | no | Starter markdown content; defaults to empty. |
| `metadata` | array of [`TemplateMetadataEntry`](schemas.md#templatemetadataentry) | no | Metadata fields the template carries. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |

## CreateTenantRequest

Body of `POST /api/admin/tenants` (deployment-admin only; see `KAIROS_DEPLOYMENT_ADMINS`).

| Field | Type | Required | Description |
|---|---|---|---|
| `initial_admin_external_id` | `string`, nullable | no | OIDC `sub` of the user to seed as the tenant's first org admin; defaults to the caller. The user must have authenticated once already (422 otherwise). |
| `name` | `string` | yes | Organization display name. |
| `slug` | `string` | yes | Organization slug (`^[a-z][a-z0-9_-]{1,62}$`). |

## CreateTransitionRequest

Body of `POST /api/boards/{id}/transitions`.

| Field | Type | Required | Description |
|---|---|---|---|
| `from_column_id` | `string` | yes | Source column (UUID). |
| `to_column_id` | `string` | yes | Target column (UUID). |

## CreatedForgeConnection

Response of connection creation and rotation: the connection plus the delivery URL and secret to paste into the forge. **The secret is shown here and nowhere else** — it is derived, never stored, so it cannot be re-read later (rotate to get a new one).

## DeleteResponse

Response of `DELETE /api/{family}/{short_code}` — the soft delete and its KAIROS-A-0001 cascade.

| Field | Type | Required | Description |
|---|---|---|---|
| `cascade_count` | `integer` | yes | How many live descendants were cascaded to (root excluded). |
| `cascaded_short_codes` | array of `string` | yes | Short codes of the cascaded descendants, sorted. |
| `short_code` | `string` | yes | The deleted item's short code. |

## DeletedResponse

`DELETE` response for both service accounts and keys.

| Field | Type | Required | Description |
|---|---|---|---|
| `deleted` | `boolean` | yes |  |
| `id` | `string` | yes |  |

## DeliveryStream

A delivery stream (`/api/delivery-streams`).

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `description` | `string`, nullable | no |  |
| `id` | `string` | yes | Stream id (UUID). |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |

## Document

A supporting document, as returned by `/api/documents`. Documents do not live on boards; they attach to a workflow item via a `supports` edge and inherit that item's board for authorization (KAIROS-A-0006). Their `lifecycle` is an editorial label (KAIROS-T-0078) — never board position.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this work was put away, RFC 3339; absent while it is live. Archiving hides work from default listings and nothing more (KAIROS-A-0020) — anything serving an archived row marks it, so an auditor never mistakes it for live work. |
| `content` | `string` | yes | Markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Creator user id (UUID). |
| `id` | `string` | yes | Entity id (UUID). |
| `lifecycle` | `string` | yes | Editorial lifecycle: `draft|review|published|archived` (KAIROS-T-0078). A label with free transitions — never a board column, never metadata. |
| `short_code` | `string` | yes | Tenant-scoped short code (`{PREFIX}-D-{NNNN}`). |
| `template_id` | `string`, nullable | no | Template the document was stamped from (UUID), if any. |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `updated_by` | `string` | yes | Last editor user id (UUID). |
| `version` | `integer` | yes | Optimistic-concurrency version (KAIROS-A-0004). |

## ErrorBody

The `error` object of [`ErrorEnvelope`].

| Field | Type | Required | Description |
|---|---|---|---|
| `code` | `string` | yes | Stable machine-readable code (`CONFLICT`, `INVALID_TRANSITION`, `FORBIDDEN`, `NOT_FOUND`, `VALIDATION`, ...). |
| `details` | `object` | yes | Structured extras (e.g. `current` on 409, `allowed_targets` on 422 `INVALID_TRANSITION`, `required_capability` on 403); `{}` when there is nothing structured to add. |
| `message` | `string` | yes | Human-readable message. |

## ErrorEnvelope

The S-0005 error envelope: `{"error": {"code", "message", "details"}}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `error` | [`ErrorBody`](schemas.md#errorbody) | yes |  |

## ForgeConnection

The webhook wiring of one repository (KAIROS-T-0106 re-key: repo identity and ownership live on the embedded repository). The webhook secret is never carried here — it is returned exactly once, by [`CreatedForgeConnection`].

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `forge` | `string` | yes | `github|gitlab` — the webhook dialect. |
| `id` | `string` | yes | Connection id (UUID) — also the webhook URL's last segment. |
| `repository` | [`RepositoryRef`](schemas.md#repositoryref) | yes | The repository this connection delivers for. |

## GraphEdge

One typed directed edge between two returned nodes.

| Field | Type | Required | Description |
|---|---|---|---|
| `depth` | `integer` | yes | Minimum view depth at which BOTH endpoints are visible. |
| `relationship` | `string` | yes | `parent|supports|informs|supersedes|blocks`. |
| `source_id` | `string` | yes | Source entity id (UUID). |
| `target_id` | `string` | yes | Target entity id (UUID). |

## GraphNode

One hydrated node of the focal subgraph.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this node was archived, RFC 3339; absent while it is live. The subgraph is archived-INCLUSIVE (KAIROS-T-0158) — omitting a node used to break the paths THROUGH it, leaving the far side floating with no route back to the focus. Clients must draw a marked node distinctly; drawing it as live is the one wrong answer. |
| `degree` | `integer` | yes | The node's TOTAL edge count — clients render `+N` where `N = degree - edges shown` for undisplayed neighbors. Archived neighbours count, because they are drawn. |
| `depth` | `integer` | yes | Minimum hop distance from the focus (0 = the focus itself). |
| `entity_type` | `string` | yes | `strategy|initiative|task|document|adr`. |
| `id` | `string` | yes | Entity id (UUID). |
| `short_code` | `string` | yes |  |
| `status` | `string` | yes | Board column name for workflow items; editorial lifecycle for documents (the A-0018 two-vocabulary split); `off-board` for ADRs without a board position. |
| `title` | `string` | yes |  |

## GraphResponse

Response of `GET /api/{family}/{code}/graph`.

| Field | Type | Required | Description |
|---|---|---|---|
| `depth` | `integer` | yes | Effective depth bound the walk used (default 2, capped at the server's MAX_TRAVERSE_DEPTH). |
| `edges` | array of [`GraphEdge`](schemas.md#graphedge) | yes | ALL edges among the returned nodes — cross-links included, not just the discovery tree. |
| `focus` | `string` | yes | The focal item's short code. |
| `nodes` | array of [`GraphNode`](schemas.md#graphnode) | yes | Every node within `depth` hops (any relationship type, either direction), focus included at depth 0; ordered by short code. Archived nodes are present and marked (`archived_at`), never dropped. |

## HistoryVersion

One row of `GET /api/{entity_type}/{short_code}/history`.

| Field | Type | Required | Description |
|---|---|---|---|
| `edited_at` | `string` | yes | RFC 3339. |
| `edited_by` | `string` | yes | Editor user id (UUID). |
| `version` | `integer` | yes | The content version this snapshot captured. |

## Initiative

An initiative (Flight Level 2), as returned by `/api/initiatives`.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this work was put away, RFC 3339; absent while it is live. Archiving hides work from default listings and nothing more (KAIROS-A-0020) — anything serving an archived row marks it, so an auditor never mistakes it for live work. |
| `board_id` | `string` | yes | Board the initiative sits on (UUID). |
| `bucket_type` | `string`, nullable | no | Bucket kind (`tech_debt|bug|ad_hoc`), set iff `is_bucket`. |
| `column_id` | `string` | yes | Current column (UUID). |
| `complexity` | `string`, nullable | no | T-shirt sizing (`xs|s|m|l|xl`). |
| `content` | `string` | yes | Markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Creator user id (UUID). |
| `id` | `string` | yes | Entity id (UUID). |
| `is_bucket` | `boolean` | yes | Whether this initiative is a bucket. |
| `short_code` | `string` | yes | Tenant-scoped short code (`{PREFIX}-I-{NNNN}`). |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `updated_by` | `string` | yes | Last editor user id (UUID). |
| `version` | `integer` | yes | Optimistic-concurrency version (KAIROS-A-0004). |

## ItemLink

One branch or pull/merge request linked to a work item.

| Field | Type | Required | Description |
|---|---|---|---|
| `author` | `string` | yes |  |
| `external_id` | `string` | yes | PR/MR number, or the branch ref. |
| `forge` | `string` | yes | `github|gitlab` (from the owning connection). |
| `forge_updated_at` | `string` | yes | The forge's own last-update time, RFC 3339. |
| `id` | `string` | yes | Link id (UUID). |
| `item_id` | `string` | yes | The linked work item (UUID). |
| `kind` | `string` | yes | `branch|pull_request`. |
| `repo_full_name` | `string` | yes | `owner/repo` (from the owning connection). |
| `state` | `string` | yes | `open|merged|closed|draft`. |
| `title` | `string` | yes |  |
| `url` | `string` | yes | Browser URL on the forge. |

## ItemMetadataResponse

Response of `GET/PATCH /api/{entity_type}/{short_code}/metadata`.

| Field | Type | Required | Description |
|---|---|---|---|
| `short_code` | `string` | yes | The item the values belong to. |
| `values` | array of [`MetadataValue`](schemas.md#metadatavalue) | yes | All metadata values on the item, ordered by definition slug. |

## ItemRelationshipsResponse

Response of `GET /api/{entity_type}/{short_code}/relationships`: both directions, grouped by relationship type.

| Field | Type | Required | Description |
|---|---|---|---|
| `incoming` | array of [`RelationshipGroup`](schemas.md#relationshipgroup) | yes | Edges where the item is the TARGET (the neighbor is the source, e.g. its parent via `parent`, items blocking it via `blocks`). |
| `outgoing` | array of [`RelationshipGroup`](schemas.md#relationshipgroup) | yes | Edges where the item is the SOURCE (the neighbor is the target, e.g. children via `parent`, items it blocks via `blocks`). |
| `short_code` | `string` | yes | The item the relationships belong to. |

## ListEnvelope_ActivityEntry

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Adr

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Board

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_DeliveryStream

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Document

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_HistoryVersion

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Initiative

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_MetadataDefinition

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_OrgMember

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Strategy

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Task

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Team

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_Template

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## ListEnvelope_TenantSummary

The S-0005 list envelope: `{items, total, limit, offset}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of `object` | yes |  |
| `limit` | `integer` | yes | The applied limit. |
| `offset` | `integer` | yes | The applied offset. |
| `total` | `integer` | yes | Rows matching the request, ignoring pagination. Live rows only unless the request asked for archived work as well — `total` and `items` always answer the same question (KAIROS-T-0159). |

## MetadataDefinition

A metadata field definition, as returned by `/api/metadata-definitions`.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `entity_types` | array of `string` | no | Entity types this definition applies to (KAIROS-T-0078): `strategy|initiative|task|document|adr`. EMPTY = applies to every type. Enforced on the write paths, not just rendering. |
| `enum_options` | array of `string` | yes | Allowed values in display order (empty unless `field_type = enum`). |
| `field_type` | `string` | yes | `string|enum|date`. |
| `id` | `string` | yes | Definition id (UUID). |
| `is_system_default` | `boolean` | yes | Seeded from the system defaults at tenant provisioning. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |

## MetadataValue

One typed metadata value on an item, hydrated with its definition.

| Field | Type | Required | Description |
|---|---|---|---|
| `definition_id` | `string` | yes | `metadata_definitions.id` (UUID). |
| `field_type` | `string` | yes | `string|enum|date`. |
| `name` | `string` | yes | The definition's display name. |
| `slug` | `string` | yes | The definition's slug (the PATCH key). |
| `value` | `string` | yes | The stored value. |

## MoveTaskRequest

Body of `POST /api/tasks/{short_code}/move` (KAIROS-I-0012): the delivery board to move the task to, by slug or UUID. It lands in that board's entry column and follows its team.

| Field | Type | Required | Description |
|---|---|---|---|
| `board` | `string` | yes |  |

## OrgDeleteResponse

Generic delete acknowledgement for organizational resources (boards, columns, transitions, teams, streams, membership rows).

| Field | Type | Required | Description |
|---|---|---|---|
| `deleted` | `boolean` | yes |  |
| `id` | `string` | yes | Id of the deleted resource (UUID). |

## OrgMember

An organization member (`GET /api/members` element).

| Field | Type | Required | Description |
|---|---|---|---|
| `display_name` | `string` | yes |  |
| `email` | `string` | yes |  |
| `external_id` | `string` | yes | OIDC `sub`. |
| `joined_at` | `string` | yes | RFC 3339. |
| `role` | `string` | yes | `admin|member`. |
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## ProgressCounts

A `(done, total)` children rollup (KAIROS-T-0080). `done` counts the children sitting in `is_done` columns.

| Field | Type | Required | Description |
|---|---|---|---|
| `done` | `integer` | yes |  |
| `has_done` | `boolean` | no | False when no board hosting the children has a done-flagged column — clients show composition only, never a done fraction. |
| `total` | `integer` | yes |  |

## RelatedItem

One hydrated neighbor of an item in the relationship graph.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this neighbour was archived, RFC 3339; absent while it is live. Relationship lists are archived-INCLUSIVE (KAIROS-T-0158): an item's edges describe what it contains and depends on, and dropping an archived endpoint silently shrinks that answer. The row therefore comes back with this marker, and every renderer must show it — ADR-20: anything serving an archived row says so, or an auditor mistakes it for live work. |
| `entity_type` | `string` | yes | `strategy|initiative|task|document|adr`. |
| `id` | `string` | yes | The neighbor's entity id (UUID). |
| `relationship_id` | `string` | yes | The edge's id (UUID) — pass to `DELETE /api/relationships/{id}`. |
| `short_code` | `string` | yes | The neighbor's short code. |
| `title` | `string` | yes | The neighbor's title. |

## Relationship

A relationship edge, as returned by `POST /api/relationships`.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Edge id (UUID). |
| `relationship` | `string` | yes | `parent|supports|informs|supersedes|blocks`. |
| `source_id` | `string` | yes | Source entity id (UUID). |
| `target_id` | `string` | yes | Target entity id (UUID). |

## RelationshipGroup

All of an item's neighbors under ONE relationship type in one direction.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of [`RelatedItem`](schemas.md#relateditem) | yes | Hydrated neighbors, in edge-creation order. |
| `relationship` | `string` | yes | `parent|supports|informs|supersedes|blocks`. |

## RemoveBoardMemberResponse

Response of `DELETE /api/boards/{id}/members/{user_id}` — full revocation of a user's board membership.

| Field | Type | Required | Description |
|---|---|---|---|
| `revoked_capabilities` | array of `string` | yes | The capabilities that were revoked (sorted). |
| `user_id` | `string` | yes | The user whose grants were revoked (UUID). |

## RemoveOrgMemberResponse

Response of `DELETE /api/members/{user_id}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `removed` | `boolean` | yes |  |
| `user_id` | `string` | yes | The removed member's user id (UUID). |

## ReplaceCapabilitiesRequest

Body of `PATCH /api/boards/{id}/members/{user_id}` — replaces the user's full capability set (revokes what is absent, grants what is new).

| Field | Type | Required | Description |
|---|---|---|---|
| `capabilities` | array of `string` | yes | The complete new capability set (at least one; use DELETE to revoke board membership entirely). |

## Repository

One repository, as returned by `/api/repositories` (KAIROS-T-0106).

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `default_branch` | `string` | yes |  |
| `delivery_board_id` | `string`, nullable | no | The owning team's delivery board (UUID) — where tasks filed against this repository land. `None` only in a misconfigured tenant (team without a delivery board). |
| `description` | `string` | yes | Short "how to work here" blurb agents read before starting. |
| `forge` | `string` | yes | `github|gitlab|other`. |
| `has_webhook` | `boolean` | yes | Whether a live webhook connection exists for it. |
| `id` | `string` | yes | Repository id (UUID). |
| `open_tasks` | `integer` | yes | Live tasks bound to this repository that are not in a done column. |
| `repo_full_name` | `string` | yes | `owner/repo` on GitHub, `group/subgroup/project` on GitLab. |
| `repo_url` | `string` | yes | Browser URL of the repository. |
| `slug` | `string` | yes | Tenant-unique slug (`^[a-z0-9][a-z0-9-]{1,62}$`). |
| `team` | [`RepositoryTeam`](schemas.md#repositoryteam) | yes | The one owning team. |
| `updated_at` | `string` | yes | RFC 3339. |

## RepositoryDetail

`GET /api/repositories/{slug}`: the repository plus its webhook connection (id only — secrets are never re-read) and in-flight links.

## RepositoryRef

The repository a task is bound to, embedded on [`super::types::Task`] so cards and agents get the slug without a second read.

| Field | Type | Required | Description |
|---|---|---|---|
| `forge` | `string` | yes | `github|gitlab|other`. |
| `id` | `string` | yes | Repository id (UUID). |
| `repo_full_name` | `string` | yes | `owner/repo` on GitHub, `group/subgroup/project` on GitLab. |
| `slug` | `string` | yes | Tenant-unique slug — what agents and the CLI address. |
| `team_id` | `string` | yes | Owning team (UUID). |

## RepositoryTeam

The owning team, embedded on [`Repository`].

| Field | Type | Required | Description |
|---|---|---|---|
| `id` | `string` | yes | Team id (UUID). |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |

## RestoreResponse

Response of `POST /api/{entity_type}/{short_code}/restore` (KAIROS-T-0160, KAIROS-A-0020): the archived item is live again. A restore deliberately does NOT un-cascade — a cascade delete was an act on a subtree, and resurrecting descendants would undo decisions nobody asked to revisit. The descendants still away are named here instead, so "I restored it and half of it is missing" is answered before it is asked.

| Field | Type | Required | Description |
|---|---|---|---|
| `short_code` | `string` | yes | The restored item's short code. |
| `still_archived_count` | `integer` | yes | How many of its descendants are still archived. |
| `still_archived_short_codes` | array of `string` | yes | Their short codes, sorted — restore them with further calls. |

## ScimTokenCreatedResponse

`POST /api/scim-tokens` response — the ONLY place the secret appears.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes |  |
| `id` | `string` | yes |  |
| `name` | `string` | yes |  |
| `token` | `string` | yes | The full bearer token. Shown once; store it in the IdP now. |

## ScimTokenListResponse

`GET /api/scim-tokens` envelope.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of [`ScimTokenView`](schemas.md#scimtokenview) | yes |  |
| `total` | `integer` | yes |  |

## ScimTokenRevokedResponse

`DELETE /api/scim-tokens/{id}` response.

| Field | Type | Required | Description |
|---|---|---|---|
| `id` | `string` | yes |  |
| `revoked` | `boolean` | yes |  |

## ScimTokenView

One row of `GET /api/scim-tokens` — never a secret, never a hash.

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes |  |
| `created_by` | `string` | yes |  |
| `id` | `string` | yes |  |
| `name` | `string` | yes |  |
| `revoked_at` | `string`, nullable | no |  |

## SearchFilter

The `filter` capability. Fields are AND with each other; array values are OR within a field.

| Field | Type | Required | Description |
|---|---|---|---|
| `board_id` | `string`, nullable | no | Restrict to items on this board (UUID; excludes documents, which do not live on boards). |
| `column_id` | `string`, nullable | no | Restrict to items in this board column (UUID; excludes documents). |
| `created_after` | `string`, nullable | no | Only items created strictly after this instant (RFC 3339). |
| `created_before` | `string`, nullable | no | Only items created strictly before this instant (RFC 3339). |
| `entity_type` | `array`, nullable | no | Restrict to these entity types (`strategy|initiative|task|document|adr`). |
| `include_deleted` | `boolean` | no | Include archived (soft-deleted) items (default false). Composes with every other capability — `q` and `traverse` included since KAIROS-T-0157 — and is a complete request on its own. Archived hits are served with `archived_at` set (KAIROS-A-0020). |
| `is_bucket` | `boolean`, nullable | no | Restrict to (non-)bucket initiatives (initiative-level attribute; other entity types are excluded). |
| `metadata` | `object`, nullable | no | Metadata conditions keyed by definition slug; string values support trailing-`*` globs (e.g. `"component": "auth*"`). All entries must match (AND). |
| `repository` | `string`, nullable | no | Restrict to tasks issued against this repository (slug or UUID — KAIROS-T-0115 made every repository reference on the wire the same shape; `repository_id` is accepted as an alias). Task-level attribute, other entity types are excluded. |
| `task_type` | `array`, nullable | no | Restrict to tasks of these types (`task|bug|tech_debt|support`; excludes non-task entities). |
| `team_id` | `string`, nullable | no | Restrict to tasks assigned to this team (UUID; task-level attribute, other entity types are excluded). |
| `work_class` | `array`, nullable | no | Restrict to tasks in these Planned/Support lanes (`planned|support`, KAIROS-T-0077; task-level attribute, other entity types are excluded). |

## SearchRequest

Body of `POST /api/search`. All fields optional; at least one of `q`/`filter`/`traverse` must be present and constraining (400 `VALIDATION` otherwise).

| Field | Type | Required | Description |
|---|---|---|---|
| `filter` | [`SearchFilter`](schemas.md#searchfilter), nullable | no |  |
| `limit` | `integer`, nullable | no | Page size (default 25, max 100). |
| `offset` | `integer`, nullable | no | Offset into the combined result set (default 0). |
| `q` | `string`, nullable | no | Full-text search (PostgreSQL websearch semantics; quoted phrases, `OR`, `-negation`). |
| `sort` | [`SearchSort`](schemas.md#searchsort), nullable | no |  |
| `traverse` | [`SearchTraverse`](schemas.md#searchtraverse), nullable | no |  |

## SearchResponse

Response of `POST /api/search`: results grouped by entity type plus the pagination envelope. `total` counts matches BEFORE `limit`/`offset` are applied.

| Field | Type | Required | Description |
|---|---|---|---|
| `limit` | `integer` | yes | The applied page size. |
| `offset` | `integer` | yes | The applied offset. |
| `results` | [`SearchResultGroups`](schemas.md#searchresultgroups) | yes | Matches grouped by entity type; empty groups are omitted from the JSON. |
| `total` | `integer` | yes | Total matches before pagination. |

## SearchResultGroups

The `results` object: one fully-typed group per entity type, each group omitted entirely when empty. Group order follows the request's combined sort.

| Field | Type | Required | Description |
|---|---|---|---|
| `adrs` | array of [`Adr`](schemas.md#adr) | no | Matching ADRs. |
| `documents` | array of [`Document`](schemas.md#document) | no | Matching documents. |
| `initiatives` | array of [`Initiative`](schemas.md#initiative) | no | Matching initiatives. |
| `strategies` | array of [`Strategy`](schemas.md#strategy) | no | Matching strategies. |
| `tasks` | array of [`Task`](schemas.md#task) | no | Matching tasks. |

## SearchSort

The `sort` clause, applied to the combined cross-type result set before pagination.

| Field | Type | Required | Description |
|---|---|---|---|
| `field` | `string` | yes | `created_at|updated_at|title|relevance`. `relevance` requires `q`. |
| `order` | `string` | yes | `asc|desc`. |

## SearchTraverse

The `traverse` capability: recursive walk of the relationship graph from a starting node.

| Field | Type | Required | Description |
|---|---|---|---|
| `depth` | `integer`, nullable | no | Maximum traversal depth — required, `1..=10` (server-capped). Typed as optional so a missing depth is a field-level 400, not a parse error. |
| `direction` | `string` | yes | Edge direction(s) to walk (`outbound|inbound|both`). |
| `from` | [`SearchTraverseFrom`](schemas.md#searchtraversefrom) | yes | The starting entity, identified by exactly one of `short_code`/`id`. |
| `relationships` | array of `string` | yes | Relationship types to follow (non-empty; `parent|supports|informs|supersedes|blocks`). |

## SearchTraverseFrom

`traverse.from`: exactly one of `short_code`/`id`.

| Field | Type | Required | Description |
|---|---|---|---|
| `id` | `string`, nullable | no | The starting entity's id (UUID). |
| `short_code` | `string`, nullable | no | The starting entity's short code (e.g. `"ACME-S-0001"`). |

## ServiceAccountListResponse

`GET /api/service-accounts` envelope.

| Field | Type | Required | Description |
|---|---|---|---|
| `items` | array of [`ServiceAccountView`](schemas.md#serviceaccountview) | yes |  |
| `total` | `integer` | yes |  |

## ServiceAccountView

A service account (never a secret).

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes |  |
| `id` | `string` | yes |  |
| `name` | `string` | yes |  |

## SetLifecycleRequest

Body of `POST /api/tasks/{short_code}/work-class` (KAIROS-T-0077): move a task between the Planned/Support lanes. Orthogonal to column Body of `PATCH /api/documents/{short_code}/lifecycle` (KAIROS-T-0078): set the document's editorial state. Free transitions; no version bump (the A-0004 contract covers title/content only).

| Field | Type | Required | Description |
|---|---|---|---|
| `lifecycle` | `string` | yes | `draft|review|published|archived`. |

## SetTaskRepositoryRequest

Body of `PUT /api/tasks/{short_code}/repository` — bind the task to a repository (slug or UUID), or clear it with `null`. The repository must be owned by the team whose delivery board the task sits on.

| Field | Type | Required | Description |
|---|---|---|---|
| `repository` | `string`, nullable | no |  |

## SetWorkClassRequest

transitions — the board rules engine is never consulted.

| Field | Type | Required | Description |
|---|---|---|---|
| `work_class` | `string` | yes | `planned|support`. |

## Strategy

A strategy (Flight Level 3), as returned by `/api/strategies`.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this work was put away, RFC 3339; absent while it is live. Archiving hides work from default listings and nothing more (KAIROS-A-0020) — anything serving an archived row marks it, so an auditor never mistakes it for live work. |
| `board_id` | `string` | yes | Board the strategy sits on (UUID). |
| `column_id` | `string` | yes | Current column (UUID). |
| `content` | `string` | yes | Markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Creator user id (UUID). |
| `hypothesis` | `string`, nullable | no |  |
| `id` | `string` | yes | Entity id (UUID). |
| `short_code` | `string` | yes | Tenant-scoped short code (`{PREFIX}-S-{NNNN}`). |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `updated_by` | `string` | yes | Last editor user id (UUID). |
| `version` | `integer` | yes | Optimistic-concurrency version (KAIROS-A-0004); submit it back on PATCH. |

## Task

A task/bug/tech-debt item (Flight Level 1), as returned by `/api/tasks`.

| Field | Type | Required | Description |
|---|---|---|---|
| `archived_at` | `string`, nullable | no | When this work was put away, RFC 3339; absent while it is live. Archiving hides work from default listings and nothing more (KAIROS-A-0020) — anything serving an archived row marks it, so an auditor never mistakes it for live work. |
| `board_id` | `string` | yes | Board the task sits on (UUID). |
| `column_id` | `string` | yes | Current column (UUID). |
| `content` | `string` | yes | Markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Creator user id (UUID). |
| `id` | `string` | yes | Entity id (UUID). |
| `repository` | [`RepositoryRef`](schemas.md#repositoryref), nullable | no |  |
| `repository_id` | `string`, nullable | no | The repository this task is issued against (UUID), if bound (KAIROS-T-0104, A-0019). |
| `short_code` | `string` | yes | Tenant-scoped short code (`{PREFIX}-T-{NNNN}`). |
| `task_type` | `string` | yes | `task|bug|tech_debt|support`. |
| `team_id` | `string`, nullable | no | Owning team (UUID), if assigned. |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `updated_by` | `string` | yes | Last editor user id (UUID). |
| `version` | `integer` | yes | Optimistic-concurrency version (KAIROS-A-0004). |
| `work_class` | `string` | yes | Planned/Support lane (`planned|support`, KAIROS-T-0077) — was this work planned, or unplanned intake? Orthogonal to `task_type`. |

## Team

A team (`/api/teams`). Every team owns a delivery board (KAIROS-A-0002/T-0010: created with the team from the system delivery defaults).

| Field | Type | Required | Description |
|---|---|---|---|
| `created_at` | `string` | yes | RFC 3339. |
| `delivery_board_id` | `string`, nullable | no | The team's delivery board (UUID); created with the team. |
| `id` | `string` | yes | Team id (UUID). |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `team_type` | `string` | yes | `stream_aligned|platform|enabling|complicated_subsystem`. |
| `updated_at` | `string` | yes | RFC 3339. |

## TeamAnnouncement

One team announcement (append-only; no edit anywhere).

| Field | Type | Required | Description |
|---|---|---|---|
| `body` | `string` | yes | Markdown body. |
| `created_at` | `string` | yes | RFC 3339. |
| `created_by` | `string` | yes | Author user id (UUID). |
| `id` | `string` | yes | Announcement id (UUID). |
| `pinned` | `boolean` | yes | Pinned announcements sort first. |
| `team_id` | `string` | yes | Owning team (UUID). |

## TeamLink

One row of a team's in-flight rollup (KAIROS-T-0101): a link plus the work item it belongs to, so the panel can link both to the forge and back into Kairos.

| Field | Type | Required | Description |
|---|---|---|---|
| `author` | `string` | yes |  |
| `external_id` | `string` | yes | PR/MR number, or the branch ref. |
| `forge` | `string` | yes | `github|gitlab`. |
| `forge_updated_at` | `string` | yes | RFC 3339. |
| `item_short_code` | `string` | yes | The Kairos work item this link belongs to. |
| `item_title` | `string` | yes |  |
| `kind` | `string` | yes | `branch|pull_request`. |
| `repo_full_name` | `string` | yes | `owner/repo`. |
| `state` | `string` | yes | `open|merged|closed|draft`. |
| `title` | `string` | yes |  |
| `url` | `string` | yes | Browser URL on the forge. |

## TeamMember

A team member (`GET /api/teams/{id}/members` element).

| Field | Type | Required | Description |
|---|---|---|---|
| `display_name` | `string` | yes |  |
| `email` | `string` | yes |  |
| `joined_at` | `string` | yes | RFC 3339. |
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## TeamPage

One node of a team's page tree, as returned by `GET /api/teams/{id}/pages` (a FLAT list — clients nest by `parent_id`, ordered by position then title).

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | yes | Markdown content (empty for folders). |
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Page id (UUID). |
| `is_protected` | `boolean` | yes | The Charter: content-editable, never renamed/moved/deleted. |
| `kind` | `string` | yes | `folder|page`. |
| `parent_id` | `string`, nullable | no | Parent folder (UUID); `null` for roots. |
| `position` | `integer` | yes | Display order among siblings. |
| `slug` | `string` | yes | URL segment, unique among siblings. |
| `team_id` | `string` | yes | Owning team (UUID). |
| `title` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |
| `version` | `integer` | yes | Optimistic-concurrency version (content edits only). |

## TeamWorkDocument

One row of `GET /api/teams/{id}/work-documents` (KAIROS-T-0084): a live document attached to the team's WORK — its `supports` parent is a task of the team or an item on the team's delivery board. Documents under org-level items deliberately do not appear.

| Field | Type | Required | Description |
|---|---|---|---|
| `lifecycle` | `string` | yes | `draft|published|adopted|superseded` (KAIROS-T-0080). |
| `parent_short_code` | `string` | yes | The supports-parent item, for attribution. |
| `parent_title` | `string` | yes |  |
| `parent_type` | `string` | yes | `strategy|initiative|task`. |
| `short_code` | `string` | yes | Document short code (links to `/items/{code}`). |
| `title` | `string` | yes |  |

## Template

A document template, as returned by `GET /api/templates`.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | yes | Starter markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Template id (UUID). |
| `is_system_default` | `boolean` | yes | Seeded from the system defaults at tenant provisioning. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |

## TemplateDetail

Response of `GET /api/templates/{id}`: the template plus its associated metadata definitions with defaults/required flags.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | yes | Starter markdown content. |
| `created_at` | `string` | yes | RFC 3339. |
| `id` | `string` | yes | Template id (UUID). |
| `is_system_default` | `boolean` | yes |  |
| `metadata` | array of [`TemplateMetadataField`](schemas.md#templatemetadatafield) | yes | The metadata fields this template carries, ordered by slug. |
| `name` | `string` | yes |  |
| `slug` | `string` | yes |  |
| `updated_at` | `string` | yes | RFC 3339. |

## TemplateMetadataEntry

One template ↔ metadata-definition association in a template write.

| Field | Type | Required | Description |
|---|---|---|---|
| `default_value` | `string`, nullable | no | Optional default, validated against the definition's type. |
| `definition_slug` | `string` | yes | Slug of an existing metadata definition. |
| `required` | `boolean` | no |  |

## TemplateMetadataField

One metadata field a template carries (`template_metadata` hydrated with its definition).

| Field | Type | Required | Description |
|---|---|---|---|
| `default_value` | `string`, nullable | no | Stamped as an `item_metadata` row at create-from-template time. |
| `definition_id` | `string` | yes | `metadata_definitions.id` (UUID). |
| `enum_options` | array of `string` | yes | Allowed values in display order (empty unless `field_type = enum`). |
| `field_type` | `string` | yes | `string|enum|date`. |
| `name` | `string` | yes | The definition's display name. |
| `required` | `boolean` | yes |  |
| `slug` | `string` | yes | The definition's slug. |

## TenantCreatedResponse

Response of `POST /api/admin/tenants` — the T-0008 provisioning report plus the seeded initial admin membership.

| Field | Type | Required | Description |
|---|---|---|---|
| `boards_created` | array of `string` | yes | Slugs of the default boards created. |
| `initial_admin` | [`TenantInitialAdmin`](schemas.md#tenantinitialadmin) | yes | The organization admin seeded at creation. |
| `metadata_definitions_copied` | `integer` | yes |  |
| `migrations_applied` | array of `string` | yes | Tenant migration versions applied. |
| `schema` | `string` | yes | The created schema (`org_{slug}`). |
| `slug` | `string` | yes |  |
| `templates_copied` | `integer` | yes |  |

## TenantDeletedResponse

Response of `DELETE /api/admin/tenants/{slug}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `dropped` | `boolean` | yes |  |
| `slug` | `string` | yes |  |

## TenantInitialAdmin

The org-admin membership created with a new tenant.

| Field | Type | Required | Description |
|---|---|---|---|
| `email` | `string` | yes |  |
| `external_id` | `string` | yes | OIDC `sub`. |
| `role` | `string` | yes | Always `admin`. |
| `user_id` | `string` | yes | `public.users.id` (UUID). |

## TenantSummary

A provisioned tenant (`GET /api/admin/tenants` element).

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | yes |  |
| `schema_exists` | `boolean` | yes | Whether the `org_{slug}` schema actually exists (drift check). |
| `slug` | `string` | yes |  |

## TransitionRequest

Body of `POST /api/{family}/{short_code}/transition`.

| Field | Type | Required | Description |
|---|---|---|---|
| `to_column_id` | `string` | yes | Target column (UUID). Must be reachable from the item's current column per the board's transition graph, else 422 `INVALID_TRANSITION` with `details.allowed_targets`. |

## UpdateBoardRequest

Body of `PATCH /api/boards/{id}` (board settings).

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |

## UpdateColumnRequest

Body of `PATCH /api/boards/{id}/columns/{col_id}` — rename, move, and/or set the done flag.

| Field | Type | Required | Description |
|---|---|---|---|
| `is_done` | `boolean`, nullable | no | Mark occupants as completed for children-progress rollups (KAIROS-T-0080). An explicit admin choice — the dead-end heuristic only ever suggests. |
| `name` | `string`, nullable | no |  |
| `position` | `integer`, nullable | no | New 0-indexed position; the other columns shift around it. |

## UpdateContentRequest

Body of `PATCH /api/{family}/{short_code}` — the KAIROS-A-0004 optimistic-concurrency content edit. `version` is the version the edit is based on; a stale value gets 409 `CONFLICT` with the current entity in `details.current`.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string` | yes | New markdown content (full replacement). |
| `title` | `string`, nullable | no | New title; omitted = keep the current title. |
| `version` | `integer` | yes | The version this edit is based on ("I'm editing version N"). |

## UpdateMetadataDefinitionRequest

Body of `PATCH /api/metadata-definitions/{id}` (org admin). Omitted fields are unchanged; `enum_options` replaces the full option list (enum definitions only).

| Field | Type | Required | Description |
|---|---|---|---|
| `entity_types` | `array`, nullable | no | Replacement scope list (KAIROS-T-0078); `Some([])` clears the scopes (definition applies to all types again). |
| `enum_options` | `array`, nullable | no | Replacement option list (enum definitions only, non-empty). |
| `name` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |

## UpdateMetadataRequest

Body of `PATCH /api/{entity_type}/{short_code}/metadata`: definition slug → value. Values are validated against the definition's type (KAIROS-A-0003: enum membership, date parse, string passthrough) and upserted; `null` clears the value. Unknown slugs are 422 `VALIDATION`.

| Field | Type | Required | Description |
|---|---|---|---|
| `values` | `object` | yes | Definition slug → new value (`null` removes the value). |

## UpdateOrgMemberRequest

Body of `PATCH /api/members/{user_id}` — role change. Demoting the last admin is rejected (422 `LAST_ADMIN`).

| Field | Type | Required | Description |
|---|---|---|---|
| `role` | `string` | yes | `admin|member`. |

## UpdateRepositoryRequest

Body of `PATCH /api/repositories/{slug}` — every field optional; `team` re-homes the repository (existing tasks are untouched; the repo → team → board rule is re-checked on their next write).

| Field | Type | Required | Description |
|---|---|---|---|
| `default_branch` | `string`, nullable | no |  |
| `description` | `string`, nullable | no |  |
| `repo_url` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |
| `team` | `string`, nullable | no | New owning team (UUID or slug). |

## UpdateStreamRequest

Body of `PATCH /api/delivery-streams/{id}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `description` | `string`, nullable | no |  |
| `name` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |

## UpdateTeamPageRequest

Body of `PATCH /api/teams/{id}/pages/{page_id}` — EITHER a version-checked content edit (`content` present, `version` required) OR a structural rename/move (`slug`/`parent_id`/`position`); mixing both in one call is a 422.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string`, nullable | no | New markdown content; requires `version`. |
| `move_to_root` | `boolean` | no | Move the node to the tree root (`parent_id` ignored when set). |
| `parent_id` | `string`, nullable | no | New parent folder id (move). JSON `null` cannot distinguish "unchanged" from "move to root" — set `move_to_root` for the latter. |
| `position` | `integer`, nullable | no | New display position. |
| `slug` | `string`, nullable | no | New sibling slug (rename). |
| `title` | `string`, nullable | no | New title (content edits only). |
| `version` | `integer`, nullable | no | The version this edit is based on (KAIROS-A-0004 pattern; 409 with `details.current` when stale). |

## UpdateTeamRequest

Body of `PATCH /api/teams/{id}`.

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |
| `team_type` | `string`, nullable | no |  |

## UpdateTemplateRequest

Body of `PATCH /api/templates/{id}` (org admin). Omitted fields are unchanged; `metadata` replaces the full association list.

| Field | Type | Required | Description |
|---|---|---|---|
| `content` | `string`, nullable | no |  |
| `metadata` | `array`, nullable | no | Replacement metadata association list. |
| `name` | `string`, nullable | no |  |
| `slug` | `string`, nullable | no |  |

