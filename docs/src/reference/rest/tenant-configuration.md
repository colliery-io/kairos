<!-- GENERATED by scripts/render-openapi.py from the OpenAPI spec.
     Do not edit: your changes will be overwritten, and CI fails when this
     file disagrees with the spec. To change it, change the
     `#[utoipa::path]` annotation on the handler. -->

# Tenant configuration

Org-admin surfaces: the metadata and template definitions that shape what work items can carry.

## metadata-definitions

### `GET /api/metadata-definitions`

List metadata definitions with their enum options and scopes (open
tenant-wide), optionally filtered to one entity type's catalog.

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |
| `entity_type` | query | no | `string` | Only definitions applying to this entity type (`strategy|initiative|task|document|adr`): unscoped definitions plus those whose scopes include it. |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_MetadataDefinition`](schemas.md#listenvelope-metadatadefinition) | Page of definitions |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad entity_type value |

### `POST /api/metadata-definitions`

Create a metadata definition (org admin only, A-0006).

Request body (required): `application/json`, [`CreateMetadataDefinitionRequest`](schemas.md#createmetadatadefinitionrequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`MetadataDefinition`](schemas.md#metadatadefinition) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Bad field_type, option rules, or duplicate slug |

### `GET /api/metadata-definitions/{id}`

Get one definition with its enum options (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Definition id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`MetadataDefinition`](schemas.md#metadatadefinition) | The definition |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |

### `DELETE /api/metadata-definitions/{id}`

Delete a definition (org admin only). Refused with 409
`DEFINITION_IN_USE` while any item value or template association
references it (S-0005 "fails if in use") — and the refusal names the
carrying work and templates, archived carriers marked (KAIROS-T-0162).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Definition id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`DeletedResponse`](schemas.md#deletedresponse) | Deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |
| `409` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Definition is in use; the message names the carrying items and templates |

### `PATCH /api/metadata-definitions/{id}`

Update a definition (org admin only): rename, re-slug, and/or replace
the enum option list. The field type is immutable — existing
`item_metadata` values were validated against it (A-0003).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Definition id (UUID) |

Request body (required): `application/json`, [`UpdateMetadataDefinitionRequest`](schemas.md#updatemetadatadefinitionrequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`MetadataDefinition`](schemas.md#metadatadefinition) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Option rules or duplicate slug |

## templates

### `GET /api/templates`

List templates (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `limit` | query | no | `integer` | Page size (default 50, max 200). |
| `offset` | query | no | `integer` | Rows to skip (default 0). |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`ListEnvelope_Template`](schemas.md#listenvelope-template) | Page of templates |

### `POST /api/templates`

Create a template (org admin only, A-0006). Metadata entries reference
existing definitions by slug; defaults are validated against the
definition's type (A-0003).

Request body (required): `application/json`, [`CreateTemplateRequest`](schemas.md#createtemplaterequest)

| Response | Body | Meaning |
|---|---|---|
| `201` | [`TemplateDetail`](schemas.md#templatedetail) | Created |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown definition slug, invalid default, or duplicate slug |

### `GET /api/templates/{id}`

Get one template: content plus its associated metadata definitions
with defaults/required flags (open tenant-wide).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Template id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`TemplateDetail`](schemas.md#templatedetail) | The template with its metadata fields |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |

### `DELETE /api/templates/{id}`

Delete a template (org admin only). Hard delete: associations cascade
and existing documents keep their content (`documents.template_id` is
set NULL by the DDL).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Template id (UUID) |

| Response | Body | Meaning |
|---|---|---|
| `200` | [`DeletedResponse`](schemas.md#deletedresponse) | Deleted |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |

### `PATCH /api/templates/{id}`

Update a template (org admin only): omitted fields are unchanged;
`metadata` replaces the full association list. Existing documents are
unaffected (A-0003: no ongoing relationship after stamping).

| Parameter | In | Required | Type | Description |
|---|---|---|---|---|
| `id` | path | yes | `string` | Template id (UUID) |

Request body (required): `application/json`, [`UpdateTemplateRequest`](schemas.md#updatetemplaterequest)

| Response | Body | Meaning |
|---|---|---|
| `200` | [`TemplateDetail`](schemas.md#templatedetail) | Updated |
| `403` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Caller is not an org admin |
| `404` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown id |
| `422` | [`ErrorEnvelope`](schemas.md#errorenvelope) | Unknown definition slug, invalid default, or duplicate slug |

